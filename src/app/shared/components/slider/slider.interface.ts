export interface ISliderConfig {
  left: {
    labelMin: string;
    labelMax: string;
    min: number;
    max: number;
    step: number;
  },
  right: {
    labelMin: string;
    labelMax: string;
    min: number;
    max: number;
    step: number;
  }
  type: string;
}
